import { getCatalog } from './src/sellibri.ts'; import { fetchProducts } from './src/odoo.ts'; import { buildDiffPayload } from './src/sync.ts'; setTimeout(async () => { try { const cat = await getCatalog(); const prods = await fetchProducts(0, 10); for (const p of prods) { const sp = cat.get(p.default_code); if (sp) { const diff = await buildDiffPayload(p, sp); if (diff) console.log('DIFF FOR', p.default_code, JSON.stringify(diff, null, 2)); } } process.exit(0); } catch(e){ console.error(e); process.exit(1); } }, 2000);
