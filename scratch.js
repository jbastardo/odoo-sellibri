import { execute } from "./src/odoo.js"; async function run() { const fields = await execute("product.template", "fields_get", []); console.log(Object.keys(fields).filter(f => f.includes("name") || f.includes("title"))); } run();
